#pragma once
#include "fibonacci.h"

template <typename T>
static Rank fibSearch(T* A, T const& e, Rank lo, Rank hi) {
	std::cout << "Fibonacci Search:\n";
	Fib fib(hi - lo);
	while (lo < hi) {
		//while()
	}
}